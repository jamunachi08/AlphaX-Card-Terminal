/*
 * AlphaX Card Terminal asset entrypoint.
 *
 * Keeping this file present prevents failures in Frappe's esbuild pipeline
 * where an app has no resolvable asset entrypoints.
 */

console.log("AlphaX Card Terminal assets loaded");
